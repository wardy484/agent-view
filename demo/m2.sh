#!/usr/bin/env bash
# demo/m2.sh — M2 (Multi-view + search) end-to-end demo.

set -euo pipefail
cd "$(dirname "$0")/.."
source "./demo/common.sh"

say "M2 demo: Kanban + Flowchart + fuzzy search — stub"
ok  "see docs/nexus-spec.md §M2 for the acceptance list."
